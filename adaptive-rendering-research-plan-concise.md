# 연구계획서(요약본): 컨텍스트 적응형 SSR/CSR 렌더링 결정

## 1. 배경 및 문제

웹 프레임워크는 SSR/CSR을 페이지 단위로 선택하지만, 이 선택은 보통 개발 시점에 정적으로 고정된다. 그러나 사용자의 기기 성능, 네트워크 품질, 서버의 순간 부하는 요청마다 크게 다르므로, 하나의 고정 전략은 특정 구간(저사양 기기, 저대역 네트워크, 트래픽 폭주)에서 성능 또는 서버 자원 효율을 손해 본다. 본 연구는 요청 컨텍스트를 입력으로 받아 **요청/세션 단위로 SSR/CSR을 동적으로 선택**하는 정책을 학습하고, 이것이 고정 전략 및 규칙 기반 정책 대비 이득이 있는지를 실증한다.

## 2. 연구 질문 및 가설

- **RQ1.** 클라이언트·네트워크·서버 부하 피처로부터 SSR/CSR 중 더 나은 선택을 예측할 수 있는가? 어떤 피처가 결정적인가?
- **RQ2.** 학습된 적응형 정책은 고정 SSR·고정 CSR·규칙 기반 정책 대비 Core Web Vitals와 서버 자원 효율에서 유의미하게 우수한가?
- **RQ3.** 통제 실험에서 학습한 정책이 재현 불가능한 실환경 조건에도 일반화되는가?

- **H1.** 서버 부하와 클라이언트 기기 등급이 최적 선택을 가르는 상위 피처다.
- **H2.** 적응형 정책은 전 구간 평균에서 최적 단일 고정 전략에 열위가 아니며, 저사양·저대역 구간에서 유의미하게 우위다.

## 3. 핵심 기여점

1. SSR/CSR 선택을 요청 컨텍스트 기반 결정 문제로 정식화.
2. 클라이언트·네트워크에 더해 **서버 실시간 부하**를 목적함수에 통합(성능과 자원 효율 동시 고려).
3. 재현 가능한 통제 조건에서 **SSR·CSR을 모두 측정하는 완전요인(factorial) 수집**으로 counterfactual 문제를 회피한 데이터셋·프로토콜 제안 및 공개.

## 4. 방법론

### 4.1 피처
- 클라이언트: 기기 유형·추정 CPU/메모리 등급, User-Agent Client Hints, `Save-Data`.
- 네트워크: `effectiveType`·`downlink`·`rtt`(2차 요청부터), 1차 요청은 지역·세션 기반 추정.
- 서버/트래픽: CPU·메모리 사용률, 동시 요청 수, SSR 렌더 큐 지연.
- 페이지 특성: SEO 필요 여부, 인터랙션 복잡도, 개인화 정도.

### 4.2 목적함수(보상)
성능 항(LCP·INP/TTI·CLS의 가중 합) + 자원 항(요청당 서버 CPU-시간, 큐 지연 페널티). 가중치는 민감도 분석으로 탐색.

### 4.3 모델링(단계적)
1. 규칙 기반 베이스라인(저사양·저대역·고부하 → SSR 등) — 이후 모든 학습 모델의 하한선.
2. 지도학습 분류/회귀 — factorial 데이터의 직접 라벨로 학습.
3. (검증 단계) Contextual bandit — 온라인 일반화 확인.

## 5. 데이터 수집 — 완전요인(factorial) 방식

핵심 아이디어: 실사용 트래픽과 달리 **테스트베드 조건은 재현 가능**하므로, 동일 조건 벡터 `x`에 대해 SSR과 CSR을 **모두 실행**하여 `r(x, SSR)`과 `r(x, CSR)`을 직접 측정한다. 이로써 "한 요청에 한 처리만 관측 가능"하다는 counterfactual 문제가 제거되고, 문제는 인과추론에서 지도학습으로 단순화되며, 조건 격자 위에서 **결정 경계를 직접 분석**할 수 있다.

수집 시 통제해야 할 3가지:

1. **서버 부하의 내생성.** 렌더 선택 자체가 서버 부하(피처)를 바꾼다. 따라서 배경 부하를 부하 생성기로 외생적으로 고정한 상태에서, 그 위에 측정 요청 하나를 얹어 잰다(제어 변수 = 배경 부하, 처리 변수 = 측정 요청의 렌더 방식).
2. **측정 변동성.** 같은 조건도 GC·지터로 값이 흔들리므로, 셀마다 SSR·CSR을 각 N회 반복하고 중앙값·p75·신뢰구간을 기록한다. 두 방식은 번갈아·무작위 순서로 실행해 시스템 드리프트가 편향으로 들어가지 않게 한다.
3. **캐시 상태.** 콜드/웜 캐시 상태를 명시적으로 고정하거나 변수로 다룬다.

> 한계: factorial 수집은 재현 가능한 합성 조건에만 적용되며 오프라인 학습·분석용이다. 실사용자 고유 조합은 재현할 수 없으므로, 실배포 검증은 온라인 방식으로 넘어간다(§6 2단계).

## 6. 실험 설계 — 2단계 하이브리드

**1단계(통제 실험, RQ1·RQ2):** 테스트베드에서 factorial 수집으로 깨끗한 counterfactual 데이터셋을 만들어 정책을 학습하고, SSR/CSR 결정 경계를 분석한다. 비교군: 고정 SSR, 고정 CSR, 규칙 기반, (상한 참고용) 사후 오라클.

**2단계(실환경 검증, RQ3):** 1단계 정책을 온라인 환경(가능 시 소규모 A/B, 아니면 contextual bandit)에 올려 재현 불가능한 실조건에서의 일반화를 검증한다.

**대표 페이지 유형(최소 3종):** 콘텐츠형(SEO 중요), 대시보드형(인터랙션 무거움·SEO 무관), 커머스형(개인화+SEO 혼재) — 유형별로 SSR/CSR 우열이 달라야 연구 전제가 성립.

**평가 지표:** LCP·INP/TTI·CLS의 분포(중앙값·p75·p95, 구간별 분해), 요청당 서버 CPU-시간, 피크 시 지연·에러율, 캐시 적중률, 오라클 대비 regret, 추론 지연 오버헤드.

## 7. 테스트베드 아키텍처(요약)

폐루프 구조: (1) 클라이언트 계층 — CPU·네트워크 스로틀링으로 조건 재현 + 부하 생성(단, 실브라우저 측정 셀과 경량 부하 셀을 분리), (2) 결정 계층(엣지/프록시) — 피처 수집 및 정책 추론, 실험별 정책 교체 스위치 보유, (3) 렌더링 백엔드 — SSR·CSR 두 경로, (4) 성능 측정(RUM) — `web-vitals` 수집, (5) 데이터 레이크 — 상관 ID로 피처·결과 조인, 탐색/정책 요청 플래그, (6) 모델 학습 — 오프라인 배치 + 온라인 밴딧 → 정책 재배포.

핵심 유의점: 부하 생성기는 실렌더를 하지 않으므로 Web Vitals 측정은 실브라우저 셀에서만; 결정 시점(서버)과 측정 시점(클라이언트) 조인을 위한 상관 ID 파이프라인이 데이터 무결성의 핵심; 조건 격자·시나리오·시드·정책 버전을 모두 코드로 버전 관리해 재현성 확보.

## 8. 예상 난점 및 대응

- 서버 부하 내생성 → 배경 부하 외생 고정(§5-1).
- 실환경 분포 이동(distribution shift) → 2단계 온라인 검증(§6).
- 동적 전환에 따른 캐시 적중률 저하 → 세션 단위 전환으로 분기 수 제한, 캐시 키에 결정 클래스 반영.
- 추론 지연 역효과 → 경량 모델·엣지 추론·결과 캐싱.

## 9. 예상 일정(예시, 6개월)

- 월 1: 문헌 조사, 문제 정식화, RQ·가설 확정.
- 월 2: 테스트베드·측정 파이프라인·규칙 기반 베이스라인 구축.
- 월 3: factorial 데이터 수집, 피처 분석(RQ1).
- 월 4: 지도학습 정책 학습 및 통제 실험(RQ2).
- 월 5: 온라인 검증(밴딧/A/B), 일반화 분석(RQ3).
- 월 6: 결과 정리·통계 검정·집필·아티팩트 공개.

## 10. 윤리 및 재현성

RUM 수집 시 개인식별정보 최소화·익명화 및 정책 준수. 실환경 검증 배포 시 특정 사용자군 성능 악화를 막는 안전장치(회로 차단기·롤백) 마련. 전 과정 코드·설정·시드 공개로 재현성 확보.
